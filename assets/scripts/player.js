/**
 * Player Constructor
 */
function Player() {
	
	if (!(this instanceof arguments.callee)) throw new Error("Please use 'new' operator when calling " + arguments.callee);
	var opt = arguments[0] || {}, pri = {}, pub = this;
	
	//Name
	//Type (Generic Player, or Dealer.)
	//Hand
	//Wallet
	
	
	
}

var P = new Player();